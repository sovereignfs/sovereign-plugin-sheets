# Roadmap — Sheets

Full requirements, data model, and the `FINANCE()` design live in the spec
(`SPEC.md`); **this doc is the source of truth for build order and status.**

Status legend: 📋 not started · 🚧 in progress · ✅ shipped

Each task is sized to be one branch + one PR, per the platform's own "one
task = one branch = one PR" convention. Tasks are sequenced — assume each
depends on the previous unless noted otherwise.

---

## MVP

| # | Task | Spec ref | Depends on | Status |
| - | ---- | -------- | ---------- | ------ |
| 1 | **Scaffold** — `manifest.json`, `package.json`, `icon.svg`, empty `/sheets` page with a static grid shell (no persistence, no formulas), `tsconfig.json` | Manifest & permissions | — | ✅ |
| 2 | **Data model + basic editing/save** — `workbooks`/`sheets` tables + migrations, workbook list/create/open, editable grid (plain text/number, no formulas yet), debounced autosave to `cells_json`, sheet tabs (add/rename/delete/reorder) | Data model | Task 1 | ✅ |
| 3 | **Formula engine integration** — wire the chosen formula engine client-side, formula bar, `=` parsing, cell refs/ranges/cross-sheet refs, the built-in function set (`SUM`, `AVERAGE`, etc.), undo/redo, error-cell display | MVP scope | Task 2 | ✅ |
| 4 | **`FINANCE()` currency conversion** — `finance_rate_cache` table, Frankfurter fetch client, `FINANCE(base, quote)` registration + async-resolve bridge, basic error handling for unsupported currency codes | The `FINANCE()` function | Task 3 | ✅ |
| 5 | **Polish** — CSV export, keyboard nav (arrows, tab/enter commit, copy/paste, fill-down), empty states, delete confirmation, save-error toasts, finalize docs | MVP scope | Task 4 | ✅ |

**MVP done when:** a user can create a workbook, add/edit sheets, type
formulas that recalculate correctly using the standard function set, pull a
live currency rate into a cell via `FINANCE()`, and export a sheet to CSV.
**Done as of task 5.**

Task 3 shipped with HyperFormula (GPLv3 free tier) as the formula engine —
see SPEC.md's "Open questions" (§1) for the licensing reasoning.

**Task 5 scope note:** "copy/paste" shipped as single-cell fill-down
(Cmd/Ctrl+D copies the active cell's raw input into the cell below), not
multi-cell range copy/paste — that needed a selection-range model this MVP's
always-editable-input grid didn't have yet. Task 23 built that model and
shipped range copy/paste (plus bulk formatting and bulk clear) on top of
it — fill-down here is unrelated and unchanged.

---

## Post-MVP (shipped)

| # | Task | Spec ref | Depends on | Status |
| - | ---- | -------- | ---------- | ------ |
| 6 | **Home redesign + workbook sharing** — `ThreeColumnLayout` home (`SheetsSidebar`: Workbooks/Inbox nav + Recent list; main content: My workbooks/Shared with me), `workbook_members` table (owner/editor/viewer), `resolveWorkbookRole()` authorization on every action, owner-only Share dialog, viewer-mode read-only grid, Inbox digest, route move to `/sheets/w/[id]` | [docs/adhoc/home-and-sharing.md](docs/adhoc/home-and-sharing.md), SPEC.md's "Workbook sharing" | Task 5 | ✅ |
| 7 | **FX-provider abstraction** — extracted `FxRateProvider` interface (`_lib/fx-rate-provider.ts`); Frankfurter is now one implementation (`frankfurterProvider`) behind it, swapped in at `actions.ts`'s single `FX_PROVIDER` binding. Currency conversion only — no behavior change, no new provider added yet. Scoped narrowly ahead of the not-yet-scheduled stock-quotes task (Post-MVP item 1 below) so that future task only has to add a new provider implementation, not touch the caching/formula-engine integration layer. Not a dependency of task 8 below — CSV import is unrelated. | SPEC.md's "The `FINANCE()` function" | Task 6 | ✅ |
| 8 | **CSV import** — single-sheet, full-replace (not merge). `_lib/csv.ts`'s `parseCsv()` (RFC 4180-ish, inverse of `cellsToCsv()`), "Import CSV" toolbar button next to "Export CSV" (`canEdit`-gated), `ConfirmDialog` naming the file/sheet, sheet grows to fit a larger CSV via new `resizeSheetAction` (capped at `MAX_IMPORT_ROW_COUNT`/`MAX_IMPORT_COL_COUNT` — renamed `MAX_ROW_COUNT`/`MAX_COL_COUNT` in task 14, once the cap applied to more than just import). No manifest permission change — client-side file read, not the platform's `data:import` portability flow. | SPEC.md's "CSV import" | Task 7 | ✅ |
| 9 | **Cell number formatting** — finishes the `CellData.fmt` enum (plain/number/currency/date) that was typed since task 1 but never wired to any UI/display logic. Format `Select` in `SheetGrid.tsx`'s toolbar; per-sheet format map tracked alongside the HyperFormula engine (`WorkbookView.tsx`), merged into `cellsJson` at every save via new `mergeCellFormats()`/`extractCellFormats()` (`_lib/cells.ts`) and rendered via new `_lib/format.ts`. **Partial scope, deliberately** — bold/italic/text color and conditional formatting are explicitly deferred, not silently dropped; see SPEC.md's "Cell number formatting" for why. | SPEC.md's "Cell number formatting" | Task 8 | ✅ |
| 10 | **Named ranges** — workbook-scoped names resolving to a formula/cell reference (e.g. `TaxRate` → `=0.08`), usable in any formula. Built directly on HyperFormula's own native named-expression API — no custom resolution logic. "Named ranges" button/dialog in `WorkbookView.tsx`'s header (`_components/NamedRangesDialog.tsx`, same list+add-form shape as `WorkbookShareDialog`); new `workbooks.named_ranges_json` column + `saveNamedRangesAction` (whole-map writes, same pattern as task 9's format overrides). Viewing is available to any role; add/remove is `canEdit`-gated. | SPEC.md's "Named ranges" | Task 9 | ✅ |
| 11 | **Cell styling + data validation** — bold/italic toggle buttons in `SheetGrid.tsx`'s toolbar (next to the format `Select`) and a per-cell soft data-validation rule (number range or list of values) via new `_components/CellValidationDialog.tsx`. Generalizes task 9's per-cell-metadata mechanism (`mergeCellFormats`/`extractCellFormats` renamed and widened to `mergeCellMetadata`/`extractCellMetadata`, `_lib/cells.ts`) to also carry `style`/`validation`. New `_lib/validation.ts`'s `isCellValueValid()` renders a visual `.invalid` indicator only — validation never blocks a commit, since the grid saves on every keystroke. Per-cell only, not range-based; text color and conditional formatting stay deferred. | SPEC.md's "Cell styling and data validation" | Task 10 | ✅ |
| 12 | **Icon + workbook URL/id polish** — swapped the plugin's Lucide icon from `table` to `sheet` (launcher tile `icon.svg`, plus every in-app `Icon name="table"` use — `SheetsSidebar`'s Workbooks nav item, and the workbook-tile banners in `HomeWorkbooksList`/Inbox — moved to `sheet`; `table` itself stays in the design system's curated icon list since Docs' rich-text editor still uses it for its table-insert menu). Workbook editor route moved from `/sheets/w/[workbookId]` to `/sheets/s/[workbookId]` (`app/w/` → `app/s/`), and new workbook/sheet ids switched from `randomUUID()` to a short, lowercase, url-safe id (`_lib/ids.ts`'s `newId()` — same `nanoid` `customAlphabet` scheme as the Docs plugin's own `_lib/ids.ts`, 21 chars from `0-9a-z`) so a shared workbook link reads shorter and case-insensitively. No migration for existing rows — ids are plain `text` primary keys, so pre-existing UUID-formatted workbook/sheet ids keep working unchanged under the new route prefix; only newly-created workbooks get the shorter id going forward. | — | Task 11 | ✅ |
| 13 | **Full-workbook JSON export/import** — a second, additive export/import format alongside CSV: a lossless whole-workbook snapshot (every sheet's values/formulas/format/style/validation, plus named ranges) as one downloadable `.json` file, since CSV can only ever carry flattened values. New `_lib/workbook-export.ts` (`WorkbookExportPayload` type, `buildWorkbookExportPayload`/`downloadWorkbookExport`, and `parseWorkbookExportPayload` — shape validation + cap enforcement, called both client-side for immediate feedback and server-side as the real trust boundary). "Export workbook" button in `WorkbookView.tsx`'s header (any role, read-only op); new `ImportWorkbookButton.tsx` on the Home page next to "New workbook" — importing always creates a **new** workbook, never an in-place overwrite, so there's nothing destructive to confirm. New `importWorkbookAction` in `actions.ts`. New `MAX_IMPORT_SHEET_COUNT`/`MAX_IMPORT_FILE_SIZE_BYTES` caps (`_lib/config.ts`) — the 800KB file-size cap stays under Next.js server actions' default 1MB request-body limit rather than taking on a platform-wide `next.config.ts` override. Deliberately not XLSX — see SPEC.md's "Full-workbook JSON export/import" for the cost/fidelity tradeoffs that decided a native format over Excel interoperability. | SPEC.md's "Full-workbook JSON export/import" | Task 12 | ✅ |
| 14 | **Sheet size: bigger default + manual growth** — new-sheet default grew `60×18` → `100×20` (`DEFAULT_ROW_COUNT`/`DEFAULT_COL_COUNT`). New "Add 50 rows"/"Add 10 columns" toolbar buttons in `SheetGrid.tsx` (`canEdit`-gated, disabled at the cap) — growth was previously only a side effect of importing a bigger file. `engine.addRows()`/`addColumns()` + `resizeSheetAction`, same mechanism CSV import already used, just triggered by a button instead of a file. `MAX_IMPORT_ROW_COUNT`/`MAX_IMPORT_COL_COUNT` (task 8) renamed to `MAX_ROW_COUNT`/`MAX_COL_COUNT` — one shared 2000×100 sheet-size ceiling for CSV import, full-workbook JSON import (task 13), and manual growth alike, no behavior change. | SPEC.md's "Sheet size: default and manual growth" | Task 13 | ✅ |
| 15 | **Workbook editor header redesign** — `WorkbookView.tsx`'s header, which had grown an unwrap-able row of buttons across tasks 10–14 (the 0.9.1 hotfix only patched the resulting overflow, not the crowding itself), rebuilt to match Docs' compact `DocumentPage` header pattern: a single `.metaBar` identity row (icon-only `←` back link + truncating `<h1>` title, replacing the old text `BackLink.tsx` + two-line header — `BackLink.tsx` deleted), a wrap-safe `.metaRight` action group (`flex: none` + `max-width: 100%` + `flex-wrap: wrap`, ported from Docs), "Delete workbook" moved into an overflow `Menu` (rare/destructive action), and a new second-row `.toolbarBar` housing Undo/Redo, mirroring Docs' own header/toolbar split. Title truncates via plain CSS ellipsis (no JS-measured mirror — Sheets' title is read-only text, not an inline-editable `<input>`, so Docs' Chromium-flex-input-rendering workaround doesn't apply here). `SheetGrid.tsx`'s own per-sheet toolbar (Bold/Italic/Validation/Add rows/Add columns/Export/Import) intentionally left untouched. | SPEC.md's "Workbook editor header redesign" | Task 14 | ✅ |
| 16 | **Layout consolidation + bottom-docked sheet tabs** — direct follow-up to task 15 on the same screen: tighter `.view` padding; Export CSV/Import CSV/the "Synced" status badge move from `SheetGrid.tsx`'s own toolbar up into the header (`SheetGrid` now exposes a `SheetGridHandle` ref — `exportCsv()`/`triggerImport()`, React 19 ref-as-prop, no `forwardRef` — plus an `onStatusChange` callback, so the header can trigger/reflect per-sheet state it doesn't own); conversely Undo/Redo and Named ranges move out of the header and into `SheetGrid.tsx`'s own toolbar (task 15's standalone `.toolbarBar` row removed outright), grouped with dividers ported from Docs' `RichTextEditor` toolbar; `SheetTabs` moves to render after `SheetGrid` and docks to the bottom of the viewport. Real fix, found on a second pass after a follow-up ask to also keep the header/toolbar/column-letter row fixed while scrolling (a first attempt using `position: sticky` on just the tab strip couldn't deliver that): `app/s/[workbookId]/page.tsx` was missing `data-plugin-fullbleed`, the platform shell's own documented opt-in (`runtime/app/(platform)/shell.module.css`) for a route that wants real viewport-bounded internal scrolling — Sheets' `(home)` route group already used it, the editor route simply never had it added. Adding it closes the whole gap at once: `SheetGrid.module.css`'s `.scroller` gets genuine internal scroll, its existing `.colHeader`/`.cornerHeader` `position: sticky; top: 0` (present since the MVP) starts working as originally intended, the header/toolbar/formula bar need no positioning changes at all, and `SheetTabs`' `position: sticky` becomes unnecessary (removed — it's a normal last flex child of a now-genuinely-bounded `.view`). Also incidentally corrects part of the padding fix: the shell's own default content padding was stacking with `.view`'s own before this, and `data-plugin-fullbleed` zeroes that too. | SPEC.md's "Layout consolidation and bottom-docked sheet tabs" | Task 15 | ✅ |
| 17 | **Export/Import menu consolidation** — the header's four separate one-format triggers (`Export CSV` icon, `Import CSV` icon, labeled `Export workbook` button) collapsed into two `Menu`-triggered buttons, `Export ▾` (Export as CSV / Export as JSON) and `Import ▾` (Import CSV / "Import as JSON (new workbook)" — label spells out the new-workbook-and-navigate-away consequence up front, a deliberate wording decision made during planning, not worked out ad hoc). `Import ▾` stays whole-button `canEdit`-gated, matching Undo/Redo. `ImportWorkbookButton.tsx`'s `renderTrigger` prop became optional, with a new React 19 ref-as-prop (`ImportWorkbookHandle { triggerImport }`, same pattern as task 16's `SheetGridHandle`) so `WorkbookView.tsx` can drive a second, trigger-less instance from the Import menu's "Import as JSON" item without duplicating the file-input/validation/`useActionState` logic already shared with the Home page's own Import button. | SPEC.md's "Export/Import menu consolidation" | Task 16 | ✅ |
| 18 | **Status badge placement** — the autosave "Synced"/"Draft"/"Error" badge moved from the header's `.metaRight` command-button row into `.titleBlock`, next to the workbook name, the same slot the viewer-only "View only" badge already occupied (mutually exclusive by role, no new CSS needed). Both describe document state, not a command, so they read as a title caption rather than sitting among Export/Import/Share — matching Docs' own `.statusLine` placement. Verified the actual "Draft" → "Synced" transition live via a timed script, not just the resting state, after an unguarded screenshot initially (and incorrectly) suggested nothing had changed. | SPEC.md's "Status badge placement" | Task 17 | ✅ |
| 19 | **Resizable columns** — per-column width overrides (`sheets.col_widths_json`, sparse map, `_lib/column-widths.ts`), four ways to resize: drag the column header's edge handle (pointer-capture based), double-click it to reset to default, keyboard Left/Right (Shift for a bigger step) once the handle has focus (WAI-ARIA "window splitter" pattern, matching `packages/ui`'s `Resizable`), and round-tripping through full-workbook JSON export/import (task 13). Grid switched to `table-layout: fixed` with an explicit `<colgroup>` as the sizing mechanism. Single whole-map persistence per resize gesture (`saveColumnWidthsAction`, same shape as task 10's `saveNamedRangesAction`). | SPEC.md's "Resizable columns" | Task 18 | ✅ |
| 20 | **Formula bar and column-width fixes** — two bugs reported directly right after task 19 shipped. `FormulaBar.module.css`'s `.input` selector raised to the compound `.bar .input`, fixing a CSS-cascade-order bug where `CodeTextarea`'s own `min-height: 180px` (sized for its usual multi-line Markdown/YAML/JSON use) could win over the intended single-line override depending on CSS-module chunk load order, rendering the formula bar as a huge box instead of one line. Separately, and more seriously: task 19's grid table had no explicit `width`, so `table-layout: fixed` shrunk it to `.scroller`'s available width instead of growing to the `<colgroup>`'s true column-width sum — silently rendering every column proportionally smaller than configured on any sheet wide enough to need horizontal scroll (virtually all of them). Fixed with `width: max-content` on `.grid`. `DEFAULT_COL_WIDTH_PX` also widened 88px → 128px — real content (`$1,500.00`, a `"Note"` header) routinely truncated at the old default. | SPEC.md's "Formula bar and column-width fixes" | Task 19 | ✅ |
| 21 | **Cell font and background color** — two more `CellStyle` fields (`color`/`bg`, 6-digit hex or absent) alongside task 11's bold/italic. Two new toolbar buttons in `SheetGrid.tsx` ("Font color"/"Fill color"), each a `@sovereignfs/ui` `Popover` + `ColorPicker` (curated swatches + native custom-color trigger, `allowNone` for "back to theme default"/"no fill"). New `_lib/cell-colors.ts`'s `CELL_COLOR_SWATCHES` — a curated palette as plugin *data*, same pattern as Kanban's board colors, deliberately excluding black/white since a cell's font sits on a background that flips between light/dark mode. Applied via inline `style` (arbitrary hex can't be a fixed CSS-module class). Zero changes needed to `_lib/workbook-export.ts`/`actions.ts` — `color`/`bg` live inside the same `CellStyle` object `cellsJson` already serializes generically. | SPEC.md's "Cell font and background color" | Task 20 | ✅ |
| 22 | **Cell color visibility fixes** — direct follow-up reported right after task 21 shipped: applied colors and the toolbar's active-color indicators both read as too subtle. `CELL_COLOR_SWATCHES` re-saturated/darkened (yellow in particular pulled to a darker amber — the hardest hue to keep legible as text at any real lightness); the toolbar's font-color underline bar and fill-color swatch both enlarged, and their border now matches the active color instead of a neutral gray. | SPEC.md's "Cell color visibility fixes" | Task 21 | ✅ |
| 23 | **Multi-cell selection** — the grid's selection model widened from a single cell to an anchor + focus corner (click+drag, shift-click, or Shift+Arrow to build a rectangle; a plain click/arrow still collapses to one cell, unchanged). Three payoffs built on it: bulk formatting (Bold/Italic/Font color/Fill color/number format applied to the whole selection in one persist call, `WorkbookView.tsx`'s new `applyMetadataPatch`), copy/cut/paste (Ctrl/Cmd+C/X/V, built on HyperFormula's own `copy`/`cut`/`paste` for values/formulas + automatic relative-reference translation, with this plugin's own per-cell metadata carried in parallel), and bulk clear (Delete/Backspace clears every selected cell's value, not its formatting). Selection renders as a translucent tint (`--sv-color-accent-subtle`) plus an outer-edge marquee border, via compound `.cell.cellSelected`-style CSS selectors specifically to avoid the cascade-order bug task 20 already found and fixed once. | SPEC.md's "Multi-cell selection" | Task 22 | ✅ |
| 24 | **Click to select, double-click to edit + cell font size** — cells were previously always-editable `<input>`s (a click dropped straight into text-edit mode); reworked so a click only selects (read-only, display value) and a double-click enters edit mode (raw value, editable), matching Google Sheets/Docs. F2 and direct typing on a selected cell also enter edit mode (preserving vs. replacing content respectively); Escape while editing reverts to the value captured when edit mode was entered. Delete/Backspace on a single selected non-editing cell now routes through task 23's `clearSelectionValues()` (previously relied on native `<input>` editing, no longer available once cells are read-only when merely selected). Separately, a new per-cell `fontSize` `CellStyle` field + toolbar `Select` (`_lib/font-sizes.ts`'s curated px list), built on task 23's `applyMetadataPatch` bulk-apply infrastructure exactly like task 21's colors; and the formula bar's font size reduced (`--sv-font-size-sm` → `--sv-font-size-xs`), both reported together in the same request. | SPEC.md's "Click to select, double-click to edit" and "Cell font size" | Task 23 | ✅ |
| 25 | **Toolbar icon polish: Bold/Italic/Fill color** — compared directly against Google Sheets' own toolbar. Bold/Italic switched from styled text (`<strong>B</strong>`/`<em>I</em>`) to dedicated Lucide icons (`bold`/`italic`, new additions to `scripts/icon-list.ts`) — Italic's styled-text glyph read as an unlabeled slash at toolbar size, a font-rendering problem a dedicated icon (explicit serif bars as vector paths) sidesteps entirely. Fill color gained a `paint-bucket` icon (also new) above its existing color bar, matching Font color's "A" + bar layout — previously a bare, iconless swatch. All four buttons switched from the generic `Button` component to the toolbar's own uniform 28px `.toolbarIconButton` treatment (already used for Undo/Redo), with a new `.toolbarIconButtonActive` class for Bold/Italic's pressed state. | SPEC.md's "Toolbar icon polish: Bold/Italic/Fill color" | Task 24 | ✅ |
| 26 | **Font color default swatch fix** — direct follow-up reported right after task 25 shipped: the Font color bar showed `transparent` for the unset/default state, the same treatment Fill color legitimately uses for "no fill" — but font color is never actually invisible, unset always resolves to a real, visible `--sv-color-text-primary` (black in light mode), so a transparent bar misleadingly implied no color was applied. Fixed by defaulting the bar's background to `var(--sv-color-text-primary)` instead of `transparent` — a live token reference, so it stays correct across light/dark mode automatically. | SPEC.md's "Font color default swatch fix" | Task 25 | ✅ |
| 27 | **Color picker auto-close on selection** — picking a swatch from the Font/Fill color popover left it open, requiring a separate dismiss; fixed at the design-system level (`@sovereignfs/ui`'s `ColorPicker`, now `0.71.0`) with a new optional `onSelectionComplete` prop fired only from the curated-swatch/"no color" clicks — deliberately not from the native custom-color `<input>`'s `onChange`, preserving the original reason that path stayed open (avoids closing mid-drag inside the browser's own color dialog). Purely additive; Kanban's own `ColorPicker` usage (a form `Dialog`, not a `Popover`) is unaffected. | SPEC.md's "Color picker auto-close on selection" | Task 26 | ✅ |
| 28 | **Home page polish: shared-heading spacing + ghost "add" tile** — "Shared with me" had no gap above its grid (only "My workbooks" had a spacing-carrying `.headingRow` wrapper); fixed by moving the margin onto `.heading` itself so both bare headings get identical spacing. Separately, a dashed ghost `NewCardTile` is now the first item in the "My workbooks" grid, opening a `Menu` ("New workbook"/"Import workbook") that drives new ref-based imperative handles on `NewWorkbookDialog`/`ImportWorkbookButton` — replacing the two separate heading-row icon buttons that did the same two things. A real overflow bug found live (the menu's default `align="right"` ran off-screen at the grid's left edge) was fixed with `align="left"`. | SPEC.md's "Home page polish: shared-heading spacing + ghost \"add\" tile" | Task 27 | ✅ |
| 29 | **Ghost tile position + sizing fix** — direct follow-up to task 28, reported against a fuller account that exposed what a 2-tile test grid hadn't: the ghost tile moved from first to last in the "My workbooks" grid, and a real sizing bug was found and fixed — it rendered as a narrow ~38px sliver instead of filling its 160px cell, because `NewCardTile`'s icon-variant CSS (`packages/ui`, `CardTile.module.css`) relied on implicit grid-stretch that doesn't reach through the intermediate `display: inline-flex` container `Popover` (task 28's `Menu` wrapper) inserts between the grid and the button. Fixed with explicit `width/height: 100%` instead of `auto` — resolves correctly against either an already-stretched wrapper or the grid track directly. `packages/ui` bumped `0.71.0` → `0.71.1` (bug fix, patch). | SPEC.md's "Ghost tile position + sizing fix" | Task 28 | ✅ |

## Post-MVP (not scheduled)

See SPEC.md's "Post-MVP" section for the full list. Roughly, in likely order
of value now that tasks 6–29 have shipped:

1. Stock/security quotes as a `FINANCE()` extension. Task 7 built the
   swappable-provider plumbing this needs; still blocked on picking an
   actual keyed provider + building the admin-secrets/Console-settings
   workflow this MVP has otherwise avoided.
2. Conditional formatting. Task 9 shipped the number-format enum, task 11
   added bold/italic, task 21 added font/background color, and task 23
   built the selection-range model conditional formatting's range-rule form
   had been waiting on — this is no longer blocked on that, just not
   designed/built yet.
3. Charts.
4. Real-time multiplayer editing (largest lift — conflict resolution,
   presence, likely a data-model change to per-cell rows; workbook sharing,
   task 6, is access control only — this is a separate, much larger
   collaboration feature).
5. XLSX import/export (either direction) — task 13 shipped a lossless
   *native* full-workbook format instead; CSV (tasks 5/8) and the native
   JSON format (task 13) are the only spreadsheet interchange formats
   supported today. XLSX would need a new dependency and real bidirectional
   format-translation work — see SPEC.md's "Full-workbook JSON
   export/import" for why it was deliberately scoped out of task 13.
