export const DEFAULT_ROW_COUNT = 60;
export const DEFAULT_COL_COUNT = 18;

/** Sheet growth ceiling for CSV import — a sane bound against a malformed/huge file, not a hard product limit. */
export const MAX_IMPORT_ROW_COUNT = 2000;
export const MAX_IMPORT_COL_COUNT = 100;
